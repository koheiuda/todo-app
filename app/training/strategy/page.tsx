import { StrategyView } from "@/components/training/strategy-view";

export const metadata = { title: "戦略・ロードマップ | ベンチMAXコーチ" };

export default function TrainingStrategyPage() {
  return <StrategyView />;
}
